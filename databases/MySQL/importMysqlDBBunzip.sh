#TAGS: #IMPORT_DATABASE_BZ2 #MYSQL #BUNZIP2
#Description: Import a database from a bz2 file directly into mysql.

#bunzip2 < database.sql.bz2 | mysql -u root -proot costes

#Description: Import a database from a gzip file directly into mysql.

#gunzip < db.gz | mysql -u root -proot dbname
